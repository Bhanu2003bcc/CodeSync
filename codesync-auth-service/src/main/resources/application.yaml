server:
  port: 8081

spring:
  application:
    name: codesync-auth-service

  datasource:
    url: jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0
    username: postgres.uapqhpnndarzmcgtbiwv
    password: DcdMIH3FmsnkMss3
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

github:
  client-id: ${GITHUB_CLIENT_ID:your-client-id}
  client-secret: ${GITHUB_CLIENT_SECRET:your-client-secret}
  redirect-uri: http://localhost:8080/api/auth/github/callback
  frontend-url: http://localhost:5173
